现在新增一个对比实验，参考论文在docs中，统计在不同专家缓存率下不同草稿长度的token接受率

# 基础配置
Qwen3-30B-A3B-Base模型，使用数据集mtbench101。

# 实验一
Mixture of Cache-Conditional Experts for Efficient Mobile Device Inference方法（简称方法M，参考文件夹Cache_Prior_Moe），在缓存中的专家情况是a0（例如缓存率0.5，每一层有一半专家在cpu、一半在gpu）

现在进行draft模型的decode（草稿模型与目标模型为同一Qwen3-30B-A3B-Base），预测专家需要为b1，用方法M使缓存专家变为a1，得到输出c1.......预测专家需要为bn，用方法M缓存专家变为an，得到输出cn（n为草稿长度）

现在进行verfiy，将真正需要的输出情况d1和c1、d2和c2、、（草稿长度）做比较，多次verfiy后分别统计不同草稿长度输出token精确匹配的接受率。

# 实验二
用量化模型的激活来预测，参考论文moe-speq

# 新增实现
已新增两组实验的可运行脚本。

详细算法逻辑、参数含义和输出指标见：

- `comparison_experiments/README.md`

## 实验一：方法 M / Cache-Prior

- `comparison_experiments/cache_prior_acceptance.py`
- `comparison_experiments/run_cache_prior_acceptance.sh`

该脚本使用 `Qwen3-30B-A3B-Base` 与 `mtbench101`，在不同专家缓存率和不同草稿长度下统计：

- `prefix_acceptance_rate`：按 speculative decoding 前缀规则统计的 token 接受率。
- `position_match_rate`：逐位置比较 `d_i` 和 `c_i` 的精确匹配率。
- `cache_hit_rate`：方法 M 模拟专家缓存的命中率。

默认对比矩阵：

- 专家缓存率：`0.25,0.5,0.75,1.0`
- 正式实验草稿长度固定：`10`
- 正式实验生成长度：`max_new_tokens=128`

正式实验不再把 `draft_len=2,4,8` 分开运行，而是在 `draft_len=10` 的每一轮中统计第 1 到第 10 个草稿 token 的接受率。输出中会包含：

- `per_position_position_match_rates`：第 i 个草稿 token 单独与目标 token 相同的比例。
- `per_position_prefix_acceptance_rates`：第 i 个草稿 token 按 speculative 前缀规则真正被接受的比例。
- `summary.csv` 中展开为 `draft_pos_1_*` 到 `draft_pos_10_*` 列。

运行方式：

```bash
sbatch comparison_experiments/run_cache_prior_acceptance.sh
```

## 实验二：MoE-SpeQ 风格 INT4 专家量化 draft

- `comparison_experiments/quantize_qwen3_moe_experts.py`
- `comparison_experiments/run_quantize_qwen3_experts.sh`
- `comparison_experiments/quantized_activation_acceptance.py`
- `comparison_experiments/run_quantized_activation_acceptance.sh`

该实验参考 MoE-SpeQ 的核心观察：量化 MoE 草稿模型能够较高保真地预测全精度模型的专家激活。实验二已经改为先生成量化 draft 权重，再加载量化权重运行接受率实验，不再使用 activation 伪量化。

量化比例参考论文：

- routed expert 内的 `gate_proj`、`up_proj`、`down_proj` 权重：对称 INT4 groupwise 量化，`group_size=128`。
- router/gate、attention、normalization、embedding、lm_head、shared expert 和其他非 expert 参数：保留全精度。

运行顺序：

```bash
sbatch comparison_experiments/run_quantize_qwen3_experts.sh
sbatch comparison_experiments/run_quantized_activation_acceptance.sh
```

默认量化模型目录：

```text
/data2/group_谈海生/lagin/models/Qwen3-30B-A3B-Base-experts-int4-g128/
```

- draft 阶段：加载预生成的 INT4 routed expert 权重生成草稿 token，并记录预测专家 lookahead。
- verify 阶段：切回全精度 expert 权重，比较目标输出 token 与草稿 token，统计接受率。
- 同时统计专家预测指标：`expert_hard_match_rate`、`expert_soft_match_rate`、`target_cache_hit_rate`。

注意：当前实验二用于验证 token 接受率和专家预测质量，不等同于论文中的高性能推理系统。论文设计的量化 draft 模型应配合 GPTQ/INT4 推理 kernel、专家预取和卸载调度来提升系统效率；当前实现是在 PyTorch 中加载 packed INT4 expert 权重，运行时反量化后调用 `linear`，所以 wall-clock 时间会更慢。正式脚本已将量化权重放到 GPU：

```bash
--quantized-weight-device cuda
```

运行方式：

```bash
sbatch comparison_experiments/run_quantized_activation_acceptance.sh
```

## 小样本烟测

烟测脚本：

- `comparison_experiments/run_smoke_tests.sh`

烟测只跑 `cache_rate=0.5`、`draft_len=1,2`、`max_samples=1`、`max_new_tokens=2`，用于检查两组代码路径是否可执行。

```bash
sbatch comparison_experiments/run_smoke_tests.sh
```

## 扩展验证

烟测通过后可提交扩展验证：

```bash
sbatch comparison_experiments/run_validation_tests.sh
```

注意：当前是 Slurm 服务器登录节点，不要在登录节点直接用 `python` 启动实验程序。需要修改实验矩阵时，编辑对应的 Slurm 脚本参数后再提交：

```bash
sbatch comparison_experiments/run_quantize_qwen3_experts.sh
sbatch comparison_experiments/run_cache_prior_acceptance.sh
sbatch comparison_experiments/run_quantized_activation_acceptance.sh
```
